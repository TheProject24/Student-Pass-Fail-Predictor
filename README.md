# Student Performance Prediction

This project aimed to build a predictive model to identify student dropout risk (Pass/Fail classification) using demographic, social, and academic data. The analysis demonstrated that a Logistic Regression model achieved a reliable baseline accuracy of on unseen data. The study further concluded that while past academic failures are the strongest risk factor, the full set of student features is necessary to maintain predictive performance.
